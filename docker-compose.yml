version: '3'

services:
  web:
    container_name: django
    build: .
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/code
    env_file:
      - /.env
    ports:
      - "8000:8000"

